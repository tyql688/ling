diff --git a/dist/floating-ui.core.esm.js b/dist/floating-ui.core.esm.js
index 778b90926845337969f3240cff2673dae82880df..4ae59f6352742889baea88303e4b89ffe1ddb8e1 100644
--- a/dist/floating-ui.core.esm.js
+++ b/dist/floating-ui.core.esm.js
@@ -97,16 +97,19 @@ async function detectOverflow(state, options) {
     height: rects.floating.height
   } : rects.reference;
   const offsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(elements.floating));
-  const offsetScale = (await (platform.isElement == null ? void 0 : platform.isElement(offsetParent))) && (await (platform.getScale == null ? void 0 : platform.getScale(offsetParent))) || {
-    x: 1,
-    y: 1
-  };
   const elementClientRect = rectToClientRect(platform.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform.convertOffsetParentRelativeRectToViewportRelativeRect({
     elements,
     rect,
     offsetParent,
     strategy
   }) : rect);
+  // A zero-length axis carries no scale; CSS zoom is uniform, so use the other axis.
+  const scaleX = rect.width ? elementClientRect.width / rect.width : 0;
+  const scaleY = rect.height ? elementClientRect.height / rect.height : 0;
+  const offsetScale = (await (platform.isElement == null ? void 0 : platform.isElement(offsetParent))) && (await (platform.getScale == null ? void 0 : platform.getScale(offsetParent))) || {
+    x: scaleX || scaleY || 1,
+    y: scaleY || scaleX || 1
+  };
   return {
     top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
     bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
diff --git a/dist/floating-ui.core.mjs b/dist/floating-ui.core.mjs
index 778b90926845337969f3240cff2673dae82880df..4ae59f6352742889baea88303e4b89ffe1ddb8e1 100644
--- a/dist/floating-ui.core.mjs
+++ b/dist/floating-ui.core.mjs
@@ -97,16 +97,19 @@ async function detectOverflow(state, options) {
     height: rects.floating.height
   } : rects.reference;
   const offsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(elements.floating));
-  const offsetScale = (await (platform.isElement == null ? void 0 : platform.isElement(offsetParent))) && (await (platform.getScale == null ? void 0 : platform.getScale(offsetParent))) || {
-    x: 1,
-    y: 1
-  };
   const elementClientRect = rectToClientRect(platform.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform.convertOffsetParentRelativeRectToViewportRelativeRect({
     elements,
     rect,
     offsetParent,
     strategy
   }) : rect);
+  // A zero-length axis carries no scale; CSS zoom is uniform, so use the other axis.
+  const scaleX = rect.width ? elementClientRect.width / rect.width : 0;
+  const scaleY = rect.height ? elementClientRect.height / rect.height : 0;
+  const offsetScale = (await (platform.isElement == null ? void 0 : platform.isElement(offsetParent))) && (await (platform.getScale == null ? void 0 : platform.getScale(offsetParent))) || {
+    x: scaleX || scaleY || 1,
+    y: scaleY || scaleX || 1
+  };
   return {
     top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
     bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
